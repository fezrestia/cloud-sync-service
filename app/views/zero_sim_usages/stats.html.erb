<p><strong>0 SIM STATS ALL LOG</strong></p>

<table>
  <tr>
    <th>Year</th>
    <th>Month</th>
    <th>Day</th>
    <th>Day Used</th>
    <th>Month Used Current</th>
  </tr>

  <% @zero_sim_stats.each do |log| %>
    <tr>
      <td><%= log.year %></td>
      <td><%= log.month %></td>
      <td><%= log.day %></td>
      <td><%= log.day_used %></td>
      <td><%= log.month_used_current %></td>
    </tr>
  <% end %>
</table>

<p>
  <%= link_to "Cloud Sync Service Root Top", root_path %>
</p>

<div>
  <%= line_chart [ {name: "2 month ago",  data: @graph_data_1},
                   {name: "Last month",   data: @graph_data_2},
                   {name: "Now",          data: @graph_data_3} ],
      id: '0 SIM Log Stats',
      label: 'Log',
      xtitle: 'Day',
      ytitle: 'MB',
      discrete: true,
      library: {
        backgroundColor: 'aliceblue',
        width: 960,
        height: 540,

        lineWidth: 2,
        colors: ['#FFFF7F', '#BFFFBF', '#7FFF7F'],
        curveType: 'none',

        pointShape: 'circle',
        pointSize: 0,
        pointVisible: 'true',

        chartArea: {
          backgroundColor: 'mintcream',
          width: 870,
          height: 440,
          left: 60,
          top: 30
        },

        hAxis: {
          slantedText: 'true',
          slantedTextAngle: 90,
          showTextEvery: 1,
          textStyle: {
            color: 'black',
          }

        },

        vAxis: {
          baseline: 500,
          baselineColor: '#FFBF7F',
          gridlines: {
            color: 'whitesmoke'

          },
          ticks: [0, 100, 200, 300, 400, 500, 600],
          maxValue: 600,
          minValue: 0,
          showTextEvery: 1,
          textStyle: {
            color: 'black',
          }

        },

        crosshair: {
          color: 'cyan',
          trigger: 'both'

        },

        legend: {
          position: 'in',
          alignment: 'start'

        },

    } %>
</div>

